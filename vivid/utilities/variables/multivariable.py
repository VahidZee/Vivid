class MultiVar:
    def __init__(
            self,

    ):
        pass
        # if self.default_set:
        #     if self.default.get('is_list', False):
        #         defaults_count = len(self.default['value']) if isinstance(
        #             self.default['value'], (tuple, list)) else 1
        #     else:
        #         defaults_count = len(self.
        #
        #         def )
        #             assert not isinstance(self.default, (list, tuple)) or

    def value(self):
        pass
